cmake_minimum_required(VERSION 3.16)
project(project_name)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 ")
if(float)
    add_executable(sinsum floatsum.c)
else()
    add_executable(sinsum doublesum.c)
endif()
target_link_libraries(sinsum m)
